<%= form_with(model: order, local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :cust_name %>
    <%= form.text_field :cust_name, id: :order_cust_name %>
  </div>

  <div class="field">
    <%= form.label :cust_email %>
    <%= form.text_field :cust_email, id: :order_cust_email %>
  </div>

  <div class="field">
    <%= form.label :fulfilled %>
    <%= form.check_box :fulfilled, id: :order_fulfilled %>
  </div>

  <div class="field">
    <%= form.label :submitted %>
    <%= form.check_box :submitted, id: :order_submitted %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
